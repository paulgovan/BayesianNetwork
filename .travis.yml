sudo: false

language: r

cache: packages

install:
  - Rscript -e 'install.packages(c("devtools","roxygen2"));devtools::install_deps("BayesianNetwork")'
script:
  - Rscript -e 'devtools::check("BayesianNetwork")'

notifications:
  email:
    on_success: change
    on_failure: change
